import App from '../app'; 

// Este arquivo é o ponto de entrada (rota '/') que o Expo Router está procurando.
// Em vez de criar um componente do Expo Router, ele exporta o seu componente principal.
export default App;